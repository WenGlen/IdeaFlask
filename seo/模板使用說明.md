# 🎯 共用模板系統使用說明

## ✨ 重大更新：自動同步模板！

現在您**不需要在兩個地方修改相同的程式碼**了！

---

## 📂 新增的檔案

```
js/templates.js  ← ✨ 這是關鍵！所有模板都在這裡
```

---

## 🎯 修改 HTML 結構的新流程

### 以前（容易出錯）：
```
1. 修改 build-seo.js 的 generateXXX() 函式
2. 修改 index.html 的 renderXXX() 函式
3. 擔心兩邊不一致... 😰
```

### 現在（自動同步）：
```
1. 只修改 js/templates.js 的模板函式 ✨
2. 執行建置
3. 完成！兩邊自動同步！😊
```

---

## 📋 可以修改的模板函式

打開 `js/templates.js`，您會看到這些函式：

| 函式名稱 | 用途 | 對應區塊 |
|---------|------|---------|
| `advantageCardTemplate()` | 優勢卡片 | 你是否也有這些困擾 |
| `portfolioItemTemplate()` | 作品項目 | 作品展示 |
| `portfolioDotTemplate()` | 作品圓點 | 作品展示切換點 |
| `aboutMeTemplate()` | 關於我 | 關於我區塊 |
| `stepButtonTemplate()` | 流程按鈕 | 製作流程左側 |
| `stepContentTemplate()` | 流程內容 | 製作流程右側 |
| `faqItemTemplate()` | FAQ 項目 | 常見問題 |

---

## 🛠️ 實際操作範例

### 範例：修改 Advantages 卡片的高度

#### 1️⃣ 打開 `js/templates.js`

#### 2️⃣ 找到 `advantageCardTemplate` 函式（第 12 行左右）

```javascript
function advantageCardTemplate(item) {
  return `
    <div class="max-w-[480px] ... 
         h-[360px] ...        ← 改這裡！
         md:h-[420px] ...">   ← 改這裡！
```

#### 3️⃣ 修改高度

```javascript
function advantageCardTemplate(item) {
  return `
    <div class="max-w-[480px] ... 
         h-[400px] ...        ← 改成 400px
         md:h-[480px] ...">   ← 改成 480px
```

#### 4️⃣ 存檔並執行建置

```bash
npm run build:seo
cp index-seo.html index.html
```

#### 5️⃣ 完成！

✅ `build-seo.js` 自動使用新模板  
✅ `index.html` 的 render 函式自動使用新模板  
✅ 兩邊完全一致！

---

## 📝 修改流程總結

```
修改 js/templates.js 中的模板
    ↓
執行 npm run build:seo
    ↓
套用 cp index-seo.html index.html
    ↓
完成！兩邊自動同步 ✨
```

---

## ⚠️ 重要提醒

### ✅ 以後要改結構，改這裡：
```
js/templates.js
```

### ❌ 不要再改這些地方：
```
build-seo.js 的 generateXXX() 函式  ← 已經改用模板
index.html 的 renderXXX() 函式      ← 已經改用模板
```

---

## 🎯 檔案修改對照表

| 要修改的內容 | 修改哪個檔案 | 需要建置？ |
|-------------|------------|----------|
| CSS 樣式 | `index.html` 的 `<style>` | ✅ 是 |
| Header/Footer 結構 | `index.html` | ✅ 是 |
| **內容區塊的 HTML 結構** | **`js/templates.js`** ✨ | **✅ 是** |
| JSON 文案內容 | `js/data/*.json` | ✅ 是 |

---

## 💡 實用技巧

### 技巧 1：在瀏覽器中即時預覽
1. 直接打開 `index.html`（不用建置）
2. 修改會即時反映（因為讀取同一個模板）
3. 確定後再執行建置

### 技巧 2：保持程式碼整潔
所有 HTML 結構都集中在 `js/templates.js`  
不會散落在多個檔案中

### 技巧 3：容易維護
以後要調整樣式，只要改一個地方！

---

## 🔍 背後原理

```
js/templates.js
    ├─→ build-seo.js 載入（Node.js 環境）
    └─→ index.html 載入（瀏覽器環境）
    
兩邊使用同一份模板 = 完全一致！
```

---

## ❓ 常見問題

### Q1：我可以刪除 build-seo.js 的 generateXXX() 嗎？
**A：不行！** 這些函式現在負責「呼叫模板」和「組合結果」。  
但您不需要修改它們了！

### Q2：我修改了 templates.js 但沒效果？
**A：記得執行建置！**
```bash
npm run build:seo
cp index-seo.html index.html
```

### Q3：要改事件綁定（例如點擊事件）怎麼辦？
**A：** 
- HTML 結構 → 改 `js/templates.js`
- 事件綁定 → 改 `index.html` 的 script 區塊

### Q4：如果我不小心改錯了怎麼辦？
**A：** 用 Git 還原，或參考備份檔案

---

## 🎉 總結

### 以前：
```
❌ 要改兩個地方
❌ 容易不一致
❌ 難以維護
```

### 現在：
```
✅ 只改一個檔案（js/templates.js）
✅ 自動同步
✅ 容易維護
```

---

**現在您可以放心修改 HTML 結構了！不會再複製錯誤了！** ✨

有任何問題歡迎隨時詢問！😊

